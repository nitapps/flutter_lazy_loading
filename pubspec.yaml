name: flutter_lazy_loading
description: Simple package Big usage, flutter lazy loading or load as scroll.
version: 1.1.1
homepage: https://nitapps.github.io
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  connectivity_plus: ^6.0.3
  flutter:
    sdk: flutter
  provider: ^6.1.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
